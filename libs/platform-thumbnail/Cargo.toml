[package]
name = "rizum-platform-thumbnail"
version = "0.4.0"
edition = "2024"

[dependencies]
silica = { path = "../silica" }

[dev-dependencies]
zip = { version = "8.2", default-features = false, features = ["deflate"] }
